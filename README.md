# The Last Closed Door

Single-page book website built with Next.js and exported as a static site.

## Development

```bash
npm install
npm run dev
```

## Production

```bash
npm run build
```

The static export is written to `out/` and can be deployed to Vercel, Netlify, or any static host.
